BUILD
1. Clone project using git clone `https://github.com/arkain123/PCMSI.git`
2. Go to project folder `cd PCMSI`
3. Add permission to execute deploy script `chmod +x deploy.sh`
4. Run deploy.sh script `./deploy.sh`

RUN
1. Build project as in previous block
2. Run using `./start.sh`
